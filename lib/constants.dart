import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(158, 13, 13, 1);
const secondaryColor = Color.fromRGBO(238, 238, 238, 1);
const black = Colors.black;
const smallSpace = SizedBox(height: 20);
const smallSpace2x = SizedBox(height: 40);
